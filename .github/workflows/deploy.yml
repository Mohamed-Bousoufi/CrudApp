name: CI/CD Pipeline
on:
  push:
    branches: [ main ]
jobs:
  build-push:
    runs-on: ubuntu-latest
    environment: Assessemnt
    steps:
      - uses: actions/checkout@v4
      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKER_HUB_TOKEN }}" | docker login -u "${{ secrets.DOCKER_HUB_USERNAME }}" --password-stdin
      - name: Debug
        run: ls -la 
      - name: Build and Push
        run: |
          docker build -t ${{ secrets.DOCKER_HUB_USERNAME }}/back-end:latest ./backend
          docker build -t ${{ secrets.DOCKER_HUB_USERNAME }}/front-end:latest ./frontend/app
          docker push ${{ secrets.DOCKER_HUB_USERNAME }}/back-end:latest
          docker push ${{ secrets.DOCKER_HUB_USERNAME }}/front-end:latest 
          

  deploy:
    needs: build-push
    runs-on: ubuntu-latest
    environment: Assessemnt
    steps:
      - name: Remote SSH Deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/mbousouf/app && \
            docker compose pull && \
            docker compose up -d
